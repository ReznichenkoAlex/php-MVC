<div class="users">
        {% for user in users %}
            <a href="/admin/profile?id={{ user.id }}">{{ user.name }}</a>
            <br>
        {% endfor %}
</div>

<style>
    .users {
        width: 250;
        margin: auto;
    }
</style>